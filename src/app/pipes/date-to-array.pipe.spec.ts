import { DateToArrayPipe } from './date-to-array.pipe';

describe('DateToArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
